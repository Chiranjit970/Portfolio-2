# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
# Note: The API key is entered by users in the app interface
# and is never stored. This file is for documentation only.

# OPENAI_API_KEY=your_api_key_here

# Application Configuration
VITE_APP_NAME="AI Resume Builder"
VITE_APP_VERSION="1.0.0"

# Note: In production, consider using environment variables
# for any sensitive configuration. However, this app is designed
# to work entirely client-side with user-provided API keys.
